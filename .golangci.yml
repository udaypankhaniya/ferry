run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gofmt
    - goimports

linters-settings:
  goimports:
    local-prefixes: ferry

issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck]
